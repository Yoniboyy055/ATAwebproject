# Customer-Status Copy Table v0.1

The authoritative fifteen-row table is recorded in `ATA_WP03C_FIFTEEN_STATE_COPY_VERIFICATION_v0.1.md` and implemented in `lib/ata/content-contract.ts`. Draft, Qualified, and Archived are internal. Every other state before Manually confirmed includes the exact non-confirmation warning.
